import React from 'react';
import styles from './Footer.module.css';
const Footer = (props) => {
    return(
        <footer className={styles.footer}>
            <p className={styles.footerText}>Code your dream</p>
        </footer>
    )
};

export default Footer;